before_script:
  - mv config/database.yml.example config/database.yml
  - psql -c 'create database todoapp_test;' -U postgres
  - rake db:migrate
language: ruby
env:
  - DB = postgres
rvm:
  - 1.9.2
  - 1.9.3
script: rake test